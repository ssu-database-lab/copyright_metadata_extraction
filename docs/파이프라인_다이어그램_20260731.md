# 시스템 파이프라인 구성도

**프로젝트** 저작권 메타데이터 추출 시스템 · 숭실대학교 DB연구실
**작성일** 2026-07-31 · **기준** 현재 구현 상태(as-built)

---

## 1. 전체 파이프라인

```mermaid
flowchart TD
    User(["사용자 브라우저 / 무하유 HF Space"])
    User -->|"업로드: PDF · 이미지 · DOCX · HWP"| API

    subgraph SERVER["⑥ API / 웹 서버 — FastAPI + SSE + 웹 UI"]
        API["PipelineOrchestrator<br/>단계 오케스트레이션 · 진행률 푸시"]
        API --> CONV["FileProcessor<br/>PDF → 페이지 이미지 변환"]
        CONV --> ROUTER{"① Modality Router<br/>파일 확장자 기준 분기"}
    end

    ROUTER -->|"문서 · 텍스트"| DOCPATH
    ROUTER -->|"이미지"| IMGPATH
    ROUTER -->|"영상 · 음성"| BLOCKED

    subgraph DOCPATH["문서 경로"]
        OCR["② OCR<br/>UniversalOCRProcessor"]
        OCR --> PARA{" "}
        PARA --> LLM["③ LLM 메타데이터 추출<br/>qwen3.5-122b-a10b"]
        PARA --> NER["④ NER — 로컬 CPU<br/>KLUE-RoBERTa-Large 1.3GB"]
    end

    subgraph IMGPATH["이미지 저작물 경로"]
        VLM["⑤ VLM 속성 추출"]
        VLM --> MAP["schema_mapping<br/>map_vlm_to_unified"]
    end

    BLOCKED["🚫 P3 가드 — 미구현<br/>영상 트랙 계획 중"]

    LLM --> CONS
    NER --> CONS
    MAP --> CONS

    subgraph CONS["⑥ 통합검증 — ConsolidationAgent"]
        FM["FieldMapper<br/>NER → LLM 필드 매핑"]
        VE["ValidationEngine<br/>형식 · 논리 검증"]
        RG["ReasoningGenerator<br/>한국어 근거 생성"]
        ARB["LLM 중재자<br/>qwen3.5-122b-a10b<br/>폴백: qwen3.5-plus"]
        FM --> VE --> RG --> ARB
    end

    CONS --> INHERIT{"계약서 메타데이터<br/>제공됨?"}
    INHERIT -->|"예"| MERGE["계약서 → 저작물 상속<br/>권리정보 28필드 · LLM 호출 없음"]
    INHERIT -->|"아니오"| OUT
    MERGE --> OUT

    OUT["통합 메타데이터 JSON — 67필드<br/>필드별 판정 · 신뢰도 · 근거"]
    OUT -->|"SSE 스트림"| User
    OUT --> STORE[("results/{request_id}/<br/>llm_metadata.json<br/>consolidated_metadata.json")]

    style BLOCKED fill:#ffe0e0,stroke:#d33,stroke-width:2px
    style NER fill:#e0f0e0,stroke:#2a2
    style CONS fill:#f0f0ff,stroke:#66c
    style OUT fill:#fff8e0,stroke:#c90
```

---

## 1-B. 목표 파이프라인 — 영상 트랙 적용 시

§1과 동일한 흐름이나, **파일 업로드부터 시작**하고 **영상·음성을 키프레임 추출을 거쳐 VLM으로 라우팅**한 구성입니다. 이미지와 영상은 **동일한 처리 구조(프레임 → 다중이미지 VLM 1회 호출 → 스키마 매핑)를 공유**하되, **모델은 서로 다릅니다**.

> **2026-08-03 갱신** — 초판에서는 이미지·영상이 동일 모델(Gemma)로 수렴한다고 보았으나, 8개 모델 × 5영상 비교시험 결과 **영상은 `qwen3.5-omni-plus`(92.3%)가 Gemma(76.9%)보다 뚜렷이 우수**해 모델을 분리했습니다. 구조는 공유하고 모델만 다르므로 코드 경로는 그대로 재사용됩니다. 근거: `docs/영상모델_비교보고서_20260803.md`

```mermaid
flowchart TD
    UPLOAD["📁 파일 업로드<br/>PDF · 이미지 · DOCX · HWP · 영상 · 음성"]
    UPLOAD --> FILE_PROCESSOR["File Processor<br/>PDF → 페이지 이미지 변환"]
    FILE_PROCESSOR --> MODALITY_ROUTER{"Modality Router<br/>파일 확장자 기준 분기"}

    MODALITY_ROUTER -->|"문서 / 텍스트<br/>(계약서 · 어문 저작물)"| OCR_STAGE
    MODALITY_ROUTER -->|"이미지 저작물"| IMAGE_VLM
    MODALITY_ROUTER -->|"영상 / 음성 저작물"| KEYFRAME_EXTRACTION

    subgraph DOCUMENT_PATH["📄 문서 경로 — 계약서 OR 어문 저작물"]
        OCR_STAGE["1️⃣ OCR<br/>Qwen3-VL-235B<br/>폴백: mistral → google → naver"]
        OCR_STAGE --> PARALLEL_SPLIT{"병렬 실행"}
        PARALLEL_SPLIT --> LLM_EXTRACTION["2️⃣ LLM 메타데이터 추출<br/>qwen3.5-122b-a10b"]
        PARALLEL_SPLIT --> NER_EXTRACTION["3️⃣ NER — 로컬 CPU<br/>KLUE-RoBERTa-Large"]
    end

    subgraph MEDIA_PATH["🎬 저작물 경로 — 처리 구조 공유 · 모델 분리"]
        KEYFRAME_EXTRACTION["4️⃣ 키프레임 추출<br/>ffmpeg · 균등앵커 + farthest-point 선별<br/>길이별 3~16장"]
        KEYFRAME_EXTRACTION --> VIDEO_VLM
        VIDEO_VLM["5️⃣-a 영상 VLM 속성 추출<br/>qwen3.5-omni-plus (92.3%)<br/>폴백: Qwen3-VL-235B → Gemma 4 31B"]
        IMAGE_VLM["5️⃣-b 이미지 VLM 속성 추출<br/>Gemma 4 31B<br/>폴백: 자체서버 → Qwen3-VL"]
        VIDEO_VLM --> SCHEMA_MAPPING
        IMAGE_VLM --> SCHEMA_MAPPING["6️⃣ 스키마 매핑<br/>→ 통합 67필드<br/>+ ffprobe 기술 메타데이터"]
    end

    LLM_EXTRACTION --> CONSOLIDATION
    NER_EXTRACTION --> CONSOLIDATION
    SCHEMA_MAPPING --> CONSOLIDATION

    CONSOLIDATION["7️⃣ 통합검증 — ConsolidationAgent<br/>FieldMapper → ValidationEngine →<br/>ReasoningGenerator → LLM 중재자"]

    CONSOLIDATION --> INHERITANCE_CHECK{"계약서 메타데이터<br/>제공됨?"}
    INHERITANCE_CHECK -->|"예"| INHERITANCE_MERGE["8️⃣ 계약서 → 저작물 상속<br/>권리정보 28필드 · LLM 호출 없음"]
    INHERITANCE_CHECK -->|"아니오"| FINAL_OUTPUT
    INHERITANCE_MERGE --> FINAL_OUTPUT

    FINAL_OUTPUT["✅ 통합 메타데이터 JSON — 67필드<br/>필드별 판정 · 신뢰도 · 근거"]
    FINAL_OUTPUT --> RESULT_STORE[("results/{request_id}/")]

    style KEYFRAME_EXTRACTION fill:#e8f0ff,stroke:#4a7,stroke-width:2px
    style VIDEO_VLM fill:#e8f0ff,stroke:#4a7,stroke-width:2px
    style IMAGE_VLM fill:#e8f0ff,stroke:#4a7,stroke-width:2px
    style NER_EXTRACTION fill:#e0f0e0,stroke:#2a2
    style CONSOLIDATION fill:#f0f0ff,stroke:#66c
    style FINAL_OUTPUT fill:#fff8e0,stroke:#c90
```

**단계 설명**

| # | 단계 | 실행 위치 | 설명 |
|---|---|---|---|
| 1️⃣ | **OCR** | Alibaba (클라우드) | 스캔 문서 → 한국어 텍스트 |
| 2️⃣ | **LLM 메타데이터 추출** | Alibaba (클라우드) | 텍스트 → 구조화 67필드 JSON |
| 3️⃣ | **NER** | **로컬 CPU** | 인명·기관·연락처 — 외부 전송 없음 |
| 4️⃣ | **키프레임 추출** | **로컬 (ffmpeg)** | 영상 전용 · API 비용 0원 |
| 5️⃣-a | **영상 VLM 속성 추출** | Alibaba (클라우드) | qwen3.5-omni-plus · 설명·주제어 |
| 5️⃣-b | **이미지 VLM 속성 추출** | OpenRouter (클라우드) | Gemma 4 31B · 설명·저작물 유형·주제어 |
| 6️⃣ | **스키마 매핑** | 로컬 | VLM 출력 + ffprobe → 통합 스키마 |
| 7️⃣ | **통합검증** | Alibaba (클라우드) | LLM↔NER 중재, 신뢰도·근거 부여 |
| 8️⃣ | **계약서 상속** | **로컬** | 순수 병합, LLM 호출 없음 |

> **문서 경로는 두 가지 역할을 모두 처리합니다**: **계약서·동의서** — 권리정보를 *제공*하는 문서, 그리고 **어문 저작물** — 권리정보를 *상속받는* 저작물. 두 경우 모두 동일한 OCR → LLM ∥ NER → 통합검증 체인을 사용하며, **상속 단계에서의 역할만 다릅니다**.

> **핵심**: 이미지 저작물은 VLM으로 직행하고, 영상 저작물은 키프레임 추출을 거친 뒤 VLM으로 들어갑니다. 두 경로는 **호출 구조·프롬프트 형식·스키마 매핑·통합검증을 100% 공유**하며 **모델만 다릅니다**(영상 `qwen3.5-omni-plus` / 이미지 Gemma 4 31B). 따라서 신규 개발은 **프레임 추출 단계와 모델 라우팅뿐**입니다.

---

## 2. 모델 백엔드 및 외부 의존성

```mermaid
flowchart LR
    subgraph LOCAL["🖥️ 로컬 실행 — 외부 전송 없음"]
        NER["④ NER<br/>KLUE-RoBERTa-Large<br/>CPU · 1.3GB"]
        SRV["⑥ FastAPI 서버<br/>파일 변환 · 오케스트레이션"]
    end

    subgraph OCRCHAIN["② OCR — 폴백 체인"]
        direction TB
        O1["1. Alibaba<br/>qwen3-vl-235b-a22b-instruct"]
        O2["2. Mistral OCR"]
        O3["3. Google Vision"]
        O4["4. Naver CLOVA"]
        O1 -.->|"실패 시"| O2 -.->|"실패 시"| O3 -.->|"실패 시"| O4
    end

    subgraph VLMCHAIN["⑤-b 이미지 VLM — 폴백 체인"]
        direction TB
        V1["1. Gemma 4 31B<br/>OpenRouter"]
        V2["2. Gemma 4 31B<br/>자체 vLLM 서버"]
        V3["3. Qwen3-VL-235B<br/>DashScope"]
        V1 -.->|"실패 시"| V2 -.->|"실패 시"| V3
    end

    subgraph VIDCHAIN["⑤-a 영상 VLM — 폴백 체인 (2026-08-03 신설)"]
        direction TB
        W1["1. qwen3.5-omni-plus<br/>DashScope · 92.3% · 5.0초"]
        W2["2. Qwen3-VL-235B<br/>DashScope · 80.8% · 4.0초"]
        W3["3. Gemma 4 31B<br/>OpenRouter · 76.9%"]
        W1 -.->|"실패 시"| W2 -.->|"실패 시"| W3
    end

    subgraph TEXTLLM["③⑥ 추출 / 통합검증"]
        T1["qwen3.5-122b-a10b<br/>DashScope"]
        T2["폴백: qwen3.5-plus"]
        T1 -.->|"실패 시"| T2
    end

    subgraph REGION["🌍 데이터 처리 위치"]
        SG["싱가포르<br/>Alibaba DashScope"]
        US["미국 +<br/>제3자 프로바이더<br/>OpenRouter"]
        OTHER["프랑스 / 미국<br/>Mistral · Google"]
    end

    OCRCHAIN --> SG
    TEXTLLM --> SG
    VLMCHAIN --> US
    VIDCHAIN --> SG
    VIDCHAIN -.->|"최종 폴백만"| US
    OCRCHAIN -.-> OTHER

    style LOCAL fill:#e0f0e0,stroke:#2a2,stroke-width:2px
    style REGION fill:#fff0f0,stroke:#d33
```

---

## 3. 계약서 + 저작물 동시 분석 흐름

`/pair` 엔드포인트는 계약서와 저작물을 **동시에** 분석한 뒤, 계약서가 완료되는 즉시 권리정보를 병합합니다.

```mermaid
sequenceDiagram
    participant U as 사용자 / 무하유
    participant S as API 서버
    participant C as 계약서 파이프라인
    participant W as 저작물 파이프라인
    participant M as 상속 병합

    U->>S: 계약서 + 저작물 N건 제출 (단일 폼)
    par 계약서 분석
        S->>C: POST /api/llm-extract (document_type=계약서)
        C->>C: OCR → LLM ∥ NER → 통합검증
        C-->>S: consolidated_metadata (권리정보)
    and 저작물 분석 (동시 최대 3건)
        S->>W: POST /api/llm-extract (contract_metadata 없이)
        W->>W: VLM → 통합검증
        W-->>S: 저작물 메타데이터 (시각 필드)
    end
    S->>M: POST /api/apply-inheritance
    Note over M: 순수 병합 — LLM 호출 없음<br/>권리정보 28필드 상속<br/>시각 필드는 절대 덮어쓰지 않음
    M-->>U: 병합 레코드<br/>CONTRACT_INHERITED (0.8) / CONTRACT_AMBIGUOUS (0.5)
```

---

## 4. 통합검증 판정 유형

```mermaid
flowchart LR
    IN["LLM 결과 + NER 결과"] --> D{"필드별<br/>비교"}
    D -->|"양쪽 일치"| A["AGREED<br/>신뢰도 0.9–1.0"]
    D -->|"값 충돌"| B["CONFLICT<br/>0.7–0.9<br/>LLM 중재"]
    D -->|"LLM만 추출"| C["LLM_ONLY<br/>0.5–0.7"]
    D -->|"NER만 추출"| E["NER_ONLY<br/>0.6–0.8"]
    D -->|"둘 다 없음"| F["MISSING<br/>0.0"]
    F -.->|"계약서 제공 시"| G["CONTRACT_INHERITED 0.8<br/>CONTRACT_AMBIGUOUS 0.5"]

    style A fill:#e0f0e0
    style B fill:#fff0e0
    style F fill:#ffe0e0
    style G fill:#e0e8ff
```

---

## 5. API 엔드포인트

| 엔드포인트 | 용도 |
|---|---|
| `POST /api/llm-extract` | **메인** — 전체 파이프라인 (문서/이미지 자동 라우팅) |
| `POST /api/apply-inheritance` | 계약서 → 저작물 권리정보 병합 (LLM 호출 없음) |
| `POST /api/ocr-universal` | OCR 단독 |
| `POST /api/ner-extract` | OCR + NER 단독 |
| `GET /pair` | 계약서+저작물 동시 검사 UI |
| `GET /v2`, `GET /` | 단일 파일 웹 UI |
| `GET /health` | 서비스 상태 |

---

## 6. 현재 구현 상태

| 구성요소 | 상태 |
|---|---|
| ② OCR | ✅ 정상 — 4개 제공자 폴백 |
| ③ LLM 추출 | ✅ 정상 |
| ④ NER | ✅ 정상 (로컬 CPU, 외부 전송 없음) |
| ⑤ 이미지 VLM | ✅ 정상 — 3단 폴백 |
| ⑥ 통합검증 | ✅ 정상 |
| 계약서 상속 | ✅ 정상 — 무하유 20필드 중 19개 충족 |
| **영상 / 음성 트랙** | ❌ **미구현** (P3 가드) — `영상트랙_구현계획_20260731.md` 참조 |
| **주민등록번호 마스킹** | ❌ **미구현** — `주민등록번호_마스킹_설계검토_20260731.md` 참조 |

*English version of this document: `docs/pipeline_diagram_20260731_EN.md`*
